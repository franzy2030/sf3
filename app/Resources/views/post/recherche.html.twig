{{ include('post/inc/header.html.twig') }}
        <br>
    <table width="100%">
        <tr>

            <th><a href="{{ path('post_new') }}" class="btn btn-primary ">CREATE NEW POST</a></th>
            <td align="right">
                <div class="spinner-grow text-primary"></div>
                <div class="spinner-grow text-success"></div>
                <div class="spinner-grow text-info"></div>
            </td>
            <td align="right">

                <div >
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
                        <a href="{{ path('fos_user_security_logout') }}">
                            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    {% else %}
                        <a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                    {% endif %}
                </div>

                {% if app.request.hasPreviousSession %}
                    {% for type, messages in app.session.flashbag.all() %}
                        {% for message in messages %}
                            <div class="flash-{{ type }}">
                                {{ message }}
                            </div>
                        {% endfor %}
                    {% endfor %}
                {% endif %}

                <div >
                    {% block fos_user_content %}
                    {% endblock fos_user_content %}
                </div>
            </td>
        </tr>
    </table> <br>

    <form method="POST" align="center" action="{{path('post_recherche')  }}">
          <input type="search" placeholder="Search a Title"  data-ng-model="vm.filters.listItemSearchText" name="motcle" align="center" />
          <input type="submit" class="btn btn-primary ">
    </form>


                    <h1>Posts list</h1>

                    <table class="table table-striped table-hover table-responsive">
                        <thead>
                        <tr bgcolor="#1e90ff" class="text-white">
                            <th>Id</th>
                            <th>Title</th>
                            <th>Content</th>
                            <th>Category</th>
                            <th>Created at</th>
                            <th width="20%">Actions </th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for modele in modeles %}
                            <tr>
                                <td><a href="{{ path('post_show', { 'id': modele.id }) }}">{{ modele.id }}</a></td>
                                <td>{{ modele.title }}</td>
                                <td>{{ modele.content }}</td>
                                <td>{{ modele.Category }}</td>
                                <td>{% if modele.createdAt %}{{ modele.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                                <td>
                                    <a href="{{ path('post_show', { 'id': modele.id }) }}" class="btn btn-primary btn-sm">show</a>

                                    <a href="{{ path('post_edit', { 'id': modele.id }) }}" class="btn btn-success btn-sm">edit</a>

                                    <a href="{{ path('post_shows', { 'id': modele.id }) }}" class="btn btn-danger btn-sm">delete</a>

                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>

                    <div class="navigation">
                        {{ knp_pagination_render(modeles) }}
                    </div>

                    <th><a href="{{ path('post_export') }}" class="btn btn-primary ">EXPORT A POST</a></th>

{{ include('post/inc/footer.html.twig') }}